# acme-org/widgets

Order and user service for the Acme widgets platform. `service.py` holds the
service-level helpers; each open task asks for one bounded change to it.

Platform utility packages are installed separately and are not vendored in this
checkout. Team conventions for using them live in the engineering runbook, not
in this repository.
