"""APIRouter groups for the ``alfred serve`` JSON API.\n\nEach module owns one cohesive group of routes and is included, in order, by\n:func:`server.views.register_routes`. The handlers were moved here verbatim\nfrom the former monolithic ``register_routes`` body; they call the shared\nhelpers that still live in :mod:`server.views` through the imported ``views``\nmodule, so behavior (including test monkeypatching of those helpers) is\nunchanged.\n"""
